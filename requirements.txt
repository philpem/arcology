# Flask and dependencies
Flask
simplejson
python-dotenv
watchdog

# SQLAlchemy
SQLAlchemy

# Flask extensions
Flask-WTF
Flask-Login
Flask-SQLAlchemy
Flask-Migrate
bootstrap-flask

# Python bcrypt password hashing
bcrypt

# Worker dependencies
requests
numpy

# Production server (optional)
# gunicorn

# PostgreSQL (optional). SQLAlchemy 2.1 uses psycopg 3 for an unqualified
# ``postgresql://`` URL; keep psycopg2 too for deployments whose existing
# configuration explicitly selects ``postgresql+psycopg2://``.
psycopg[binary]
psycopg2-binary

# Natural sort
natsort

# S3-compatible object storage (optional, for distributed workers)
boto3

# YAML parsing (used by api_docs blueprint to serve openapi.json)
PyYAML

# Markdown rendering (used by in-app help pages)
Markdown

# OAuth 2.0 / OpenID Connect (SSO via Keycloak, Okta, Azure AD, etc.)
authlib

# Test reporting (JUnit XML output for CI)
unittest-xml-reporting

# Sentry API (optional)
# Upper bound (against house style of leaving versions open) because the
# traces_sampler in myapp/app.py reads the request path from the 2.x sampling
# context key "wsgi_environ", set by the WSGI integration.  If that shape
# changes the sampler silently stops matching and every worker poll gets traced
# again -- which exhausts the span quota within days, with no error to notice.
# Before lifting this, re-check what the sampler is handed and update it.
sentry-sdk<3

# Optional: byte-level fuzzy hashing (TLSH) for near-duplicate file detection.
# Installed in the web and worker Docker images (which have a C++ toolchain);
# left out of this list so CI / minimal installs do not need to build it.  The
# code degrades gracefully when absent (see arcology_shared/fuzzyhash.py).
# For CI, see ci/requirements-tlsh.txt.
